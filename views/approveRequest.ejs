<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <title>DAILY Blogs</title>

</head>
<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <p class="navbar-brand">DAILY Blogs</p>
    </div>
      <ul class="nav navbar-nav navbar-right">
        <li id="home"><a href="/admin-portal">HOME</a></li>
      </ul>
  </div>
</nav>
<body>
  <div class="content">
<style>
    .blog{
      position: relative;
        width:100%;
        height:65vh;
        overflow-y: scroll;
    }
    .blogTitle{
        text-decoration: underline;
        margin-bottom:2rem;
    }
    .blogDate{
        color: gray;
        margin-bottom:2rem;
    }
    .blogDescription{
        text-align: justify;
        margin-bottom:2rem;
        padding-right:1.5rem;
    }
    .blogContent{
        padding-right:1.5rem;
        text-align: justify;
    }
    .approve-btn{
      position: absolute;
      bottom:0px;
      width:10%;
    }
    .description-text-header{
        text-align: justify;
        text-decoration: underline;
        text-decoration-line: 1px solid gray;
        font-size:18px;
        font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        color: gray;
    }
    .author-name{
        font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        font-size:1em;
        color: cadetblue;
        padding: 0px;
        margin:0px;
    }
    .date{
        font-size:14px;
        color: gray;
        padding: 0px;
        margin:0px;
        padding:.5rem;
    }
    .approve-footer{
      display: flex;
      align-items: center;
      justify-content: center;
      position: absolute;
      bottom: 0px;
      width:100%;
      height:5vh;
      background-color:rgb(234, 242, 249);
    }
    .approve-footer p{
      padding: 0px;
      margin: 0px;
      color:rgb(42, 42, 46);
      font-size:14px;
      font-weight: 700;
    }
</style>
<h1 class="author-name"><%= author %></h1>
<p class="date"><%= request.dateOfRequest %></p>
<div class="blog">
    <h2 class="blogTitle"><%= article.title %></h2>
    <p class="blogDate"><%= article.createdAt.toLocaleDateString() %></p>
    <p   class="blogDescription"><span class="description-text-header">Description</span><br><%= article.description %></p>
    <div class="blogContent">
    <% formattedBlogContent.forEach((paragraph) =>{ %>
        <p ><span class="description-text-header">Content</span><br><%= paragraph %></p>
    <% }) %>
    </div>
    <a class="btn btn-success approve-btn" href="/publish/approve-request/<%= requestId %>/<%= request._id %>">Approve</a>
</div>
</div>
<div class="approve-footer">
<p>Think and Write & Write and Think</p>
</div>
</body>
</html>