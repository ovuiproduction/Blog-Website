<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>DAILY Blogs</title>
  </head>
  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <p class="navbar-brand">DAILY Blogs</p>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li id="home"><a href="/admin-portal/dashboard">HOME</a></li>
      </ul>
    </div>
  </nav>
  <body>
    <div class="content">
      <style>
        .blog {
          width: 100%;
          height: 70vh;
          overflow-y: scroll;
        }
        .blogTitle {
          text-decoration: underline;
          margin-bottom: 2rem;
        }
        .blogDate {
          color: gray;
          margin-bottom: 2rem;
        }
        .blogDescription {
          text-align: justify;
          margin-bottom: 2rem;
          padding-right: 1.5rem;
        }
        .blogContent {
          padding-right: 1.5rem;
          text-align: justify;
        }
        .description-text-header {
          text-align: justify;
          text-decoration: underline;
          text-decoration-line: 1px solid gray;
          font-size: 18px;
          font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
            Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
            sans-serif;
          color: gray;
        }
      </style>

      <div class="blog">
        <h2 class="blogTitle"><%= article.title %></h2>
        <p class="blogDate"><%= article.createdAt.toLocaleDateString() %></p>
        <p class="blogDescription">
          <span class="description-text-header">Description</span><br /><%=
          article.description %>
        </p>
        <div class="blogContent">
          <% formattedBlogContent.forEach((paragraph) =>{ %>
          <p>
            <span class="description-text-header">Content</span><br /><%=
            paragraph %>
          </p>
          <% }) %>
        </div>
      </div>
      <%-include("partials/footer") %>
    </div>
  </body>
</html>
